<Window x:Class="MediaConverterToMP3.Views.ErrorDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Error" Height="380" Width="600"
        Background="Transparent"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True">
            <Border Background="{StaticResource SurfaceColor}"
            BorderBrush="#FF4444"
            BorderThickness="2"
            CornerRadius="25"
            Padding="40">
        <Border.Effect>
            <DropShadowEffect Color="#FF0000" Direction="270" ShadowDepth="12" BlurRadius="30" Opacity="0.5"/>
        </Border.Effect>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header with Icon -->
            <StackPanel Grid.Row="0" Margin="0,0,0,30" HorizontalAlignment="Center">
                <Border Background="#FF4444" 
                        CornerRadius="50" 
                        Width="70" 
                        Height="70" 
                        HorizontalAlignment="Center"
                        Margin="0,0,0,15">
                    <TextBlock Text="✕"
                               FontSize="35"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center"
                               Foreground="White"
                               FontWeight="Bold"/>
                </Border>
                <TextBlock x:Name="TitleText"
                           Text="Download Error" 
                           FontSize="24" 
                           FontWeight="Bold" 
                           Foreground="#FF6B6B"
                           HorizontalAlignment="Center"/>
            </StackPanel>

            <!-- Message -->
            <Border Grid.Row="1"
                    Background="{StaticResource CardColor}"
                    CornerRadius="15"
                    Padding="20"
                    Margin="0,0,0,20"
                    BorderThickness="1"
                    BorderBrush="#333333">
                <ScrollViewer VerticalScrollBarVisibility="Auto" MaxHeight="180">
                    <TextBlock x:Name="MessageText"
                               Text="The URL you entered is not valid for the selected source." 
                               FontSize="15" 
                               Foreground="{StaticResource TextColor}"
                               TextWrapping="Wrap"
                               LineHeight="28"/>
                </ScrollViewer>
            </Border>

            <!-- Buttons -->
            <StackPanel Grid.Row="2" 
                        Orientation="Horizontal" 
                        HorizontalAlignment="Center">
                <Button x:Name="OkButton"
                        Content="OK" 
                        Style="{StaticResource PrimaryButtonStyle}"
                        Height="45" 
                        MinWidth="140"
                        FontSize="15"
                        FontWeight="SemiBold"
                        Padding="25,10"
                        Click="OkButton_Click"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>

